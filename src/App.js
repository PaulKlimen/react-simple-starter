import styled from 'styled-components';

const Wrapper = styled.div`

`;

const App = () => {
    return(
      <Wrapper>
        <h1> Hello, World! </h1>
      </Wrapper>
    );
};

export default App;
